# 📊 Visualisations (Streamlit)

This folder contains **Streamlit dashboards** for interactively exploring the results generated by the PDP framework.  
The main app, `app_PDP_results.py`, visualizes distance matrices, clustering results, and configuration-level movement data.

---

## 🚀 Quick start

### 1️⃣ Install dependencies
```
pip install -r requirements.txt
```

### 2️⃣ Open and update file paths

In `app_PDP_results.py`, update the default paths defined in the sidebar section:

```
# --- Example: adjust these paths to your local setup ---

base_folder = r"C:\Users\<username>\PDP-Analysis\results"
dataset_path = os.path.join(base_folder, "N_C_Dataset.csv")
```

💡 **Tip for Windows:**  
Use raw strings (prefix the path with `r"..."`) to avoid backslash issues, for example:  
`r"C:\Users\<username>\PDP-Analysis\results"`

💡 **Tip for macOS / Linux:**  
Use forward slashes instead of backslashes, for example:  
`"/home/<username>/PDP-Analysis/results"`

Make sure the folder you specify (`base_folder`) contains:
- The generated **PDP distance matrices**, such as  
  `N_C_PDPg_fundamental_DistanceMatrix.csv`  
  `N_C_PDPg_buffer_DistanceMatrix.csv`  
  `N_C_PDPg_rough_DistanceMatrix.csv`  
  `N_C_PDPg_bufferrough_DistanceMatrix.csv`
- The dataset file `N_C_Dataset.csv`

---

### 3️⃣ Run Streamlit

Run the dashboard from your terminal:

```
python -m streamlit run "app_PDP_results.py"
```

Once launched, use the sidebar to:
- Select the **distance matrix type** (Fundamental, Buffer, Rough, Buffer + Rough)
- Choose a **visualization view**, such as:
  - 🌡️ Heatmap  
  - 🌳 Hierarchical Clustering  
  - 📉 MDS & t-SNE  
  - 🧭 Per Configuration  
  - ⚖️ Inequality Matrices  
  - 🏆 TopK Results  

---

## ⚙️ Expected inputs

The app expects outputs generated by the PDP scripts in `../scripts`:

- `N_C_Dataset.csv` — configuration data (columns: con, tst, poi, x, y)  
- `N_C_PDPg_*_DistanceMatrix.csv` — PDP distance matrices  

If these files are missing, activate HeatMap, HClust, TopK, Inequality Matrices,... in your av.py:

---

## 💡 Notes

- Large matrices may take time to load; Streamlit caches them automatically (`@st.cache_data`).
- To ensure smooth animation playback, close heavy background applications.
- The layout is optimized for wide screens — use full-screen mode for the best experience.

---
